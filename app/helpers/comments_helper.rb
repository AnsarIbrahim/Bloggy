module CommentsHelper
  # Comment button helper
  # def comment_button(commentable)
  #     if commentable.comments.count > 0
  #         link_to "#{commentable.comments.count} Comments", commentable
  #     else
  #         link_to "Comment", commentable
  #     end
  # end
end
